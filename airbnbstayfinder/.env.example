NODE_ENV=development# App environment. Common values: development | productionPORT=3000# Next.js port (only relevant if you run a custom server; usually not required in dev)DATABASE_URL=mysql://USER:PASSWORD@HOST:3306/DB_NAME# Database connection string (Prisma/MySQL)# Example: mysql://root:password@127.0.0.1:3306/airbnbstayfinderNEXTAUTH_URL=http://localhost:3000# App base URL (NextAuth)NEXTAUTH_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET# Secret used to sign sessions/tokens (NextAuth). Use a long random value in productionAIRBNB_HTTP_BASE_URL=http://localhost:8001# Base URL for your backend/provider service (scraping/search endpoints)# Example: http://localhost:8001 (dev) | https://api.yourdomain.com (prod)AI_PROVIDER=ollama# AI provider selected by your factory: ollama | openaiAI_MODEL=deepseek-r1:1.5b# Default model for the selected provider# ollama examples: deepseek-r1:1.5b, llama3.1, mistral, etc# openai examples: gpt-5-mini, gpt-5, etcOLLAMA_BASE_URL=http://127.0.0.1:11434# Ollama base URL (only used when AI_PROVIDER=ollama)OPENAI_API_KEY=# OpenAI API key (only used when AI_PROVIDER=openai)# Never expose this as NEXT_PUBLIC_*OPENAI_BASE_URL=https://api.openai.com/v1# OpenAI base URL (usually you don't need to change this)